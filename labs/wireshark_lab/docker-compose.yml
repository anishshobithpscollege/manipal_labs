version: '3.8'

services:
  wireshark-lab:
    build: .
    container_name: wireshark_login_lab
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./index.php:/var/www/html/index.php
      - ./dashboard.php:/var/www/html/dashboard.php
    restart: unless-stopped
